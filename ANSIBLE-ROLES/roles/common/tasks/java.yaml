- name: Installing Maven
  ansible.builtin.package:
    name: maven
    state:  present

- name: Installing Dependencies
  ansible.builtin.shell: mvn clean package ; "mv target/{{component}}-{{component_version}}.jar {{component.jar}}" ;
  args:
    chdir:  /app